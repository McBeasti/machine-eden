{
  "install": "sudo apt-get update -qq && sudo apt-get install -y -qq python3.12-venv python3-pip\ncd backend && python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt\ncd ../frontend && npm install",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && . .venv/bin/activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev -- --host 0.0.0.0 --port 5173"
    }
  ]
}
